<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#000000</item>
        <item name="android:datePickerDialogTheme">@style/CustomDateTimePicker</item>
        <item name="android:timePickerDialogTheme">@style/CustomDateTimePicker</item>
    </style>

    <style name="CustomDateTimePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">#b71c1c</item>
    </style>

</resources>
