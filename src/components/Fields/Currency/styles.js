import {StyleSheet} from 'react-native';
import {regular} from 'styles/font';

export default StyleSheet.create({
  text: {
    paddingLeft: 10,
    paddingTop: 10,
    fontSize: regular,
  },
  container: {
    width: '100%',
  },
});
